import type { Metadata } from "next";
import { Lato } from "next/font/google";
import "./globals.css";

const lato = Lato({
  variable: "--font-lato",
  subsets: ["latin"],
  weight: ["400", "700", "900"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Muslim Travel Dashboard | CrescentRating",
  description:
    "Interactive dashboard displaying key global Muslim travel statistics, market insights, and destination rankings based on CrescentRating data.",
  keywords: [
    "Muslim travel",
    "halal tourism",
    "CrescentRating",
    "Muslim-friendly destinations",
    "travel statistics",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${lato.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
